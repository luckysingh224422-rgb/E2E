services:
  - type: web
    name: facebook-messenger-bot
    env: node
    plan: free
    buildCommand: chmod +x build.sh && ./build.sh && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
